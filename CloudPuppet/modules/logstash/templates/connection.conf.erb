input {
	redis {
		host=> "127.0.0.1"
		port => 6379
		key=> "logstash:beaver"
		data_type=> "list"
	}
}

output {
  	elasticsearch {
    	host => "localhost"  
	}
  	stdout {
    		codec => rubydebug
  	}
}